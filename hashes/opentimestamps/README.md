# OpenTimestamps Proofs

OpenTimestamps proofs will be stored under `YYYY/MM/` when available.

Rows without a proof remain `timestamp_pending` until proofing succeeds or a correction documents the failure.
